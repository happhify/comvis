# torch/torchvision sudah ada di base image (pytorch/pytorch:*-cuda*-runtime)
# dengan build CUDA yang benar -> sengaja TIDAK dicantumkan di sini supaya
# pip tidak menimpanya dengan wheel CPU-only dari PyPI.
ultralytics>=8.2,<9.0
opencv-python-headless>=4.9,<5.0
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0
requests>=2.31,<3.0
